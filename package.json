{
	"name": "jul-compiler",
	"version": "0.0.11",
	"description": "Compiler cli for JUL",
	"main": "out/cli.js",
	"type": "module",
	"bin": {
		"jul": "out/cli.js"
	},
	"author": {
		"name": "Julian Xander"
	},
	"keywords": [
		"jul",
		"compiler",
		"language"
	],
	"license": "ISC",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/JulianXander/jul-compiler.git"
	},
	"scripts": {
		"build": "npx tsc && copyfiles --flat src/core-lib.jul out",
		"build-all": "npm run build && npx tsc --project ../jul-language-server/tsconfig.json && npx tsc --project ../vscode-jul-language-service/tsconfig.json",
		"build-all-and-deploy": "npm run build-all && npm run install-cli && npm run test-deploy --prefix ../vscode-jul-language-service",
		"test": "tsc -p tsconfig.test.json && copyfiles --flat src/core-lib.jul test && mocha --require ./test-setup.mjs \"test/**/*.test.js\"",
		"install-cli": "npm i -g"
	},
	"devDependencies": {
		"@types/chai": "^5.2.3",
		"@types/mocha": "^10.0.10",
		"@types/node": "^24.13.3",
		"chai": "^6.2.2",
		"chai-as-promised": "^8.0.2",
		"mocha": "^12.0.0",
		"ts-node": "^10.9.2"
	},
	"dependencies": {
		"ajv": "^8.20.0",
		"js-yaml": "^5.4.1",
		"typescript": "^5.9.3",
		"webpack": "^5.110.3",
		"webpack-shebang-plugin": "^1.1.8"
	}
}
